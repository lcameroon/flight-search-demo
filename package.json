{
  "name": "fse",
  "private": true,
  "version": "0.0.1",
  "description": "Flight Search Engine using AngularJS Material",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "jasmine-core": "^2.3.4",
    "karma": "^0.12.37",
    "karma-chrome-launcher": "^0.2.0",
    "karma-jasmine": "^0.3.5",
    "karma-junit-reporter": "^0.2.2",
    "protractor": "^2.0.0",
    "selenium-webdriver": "^2.45.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "start": "http-server -a localhost -p 8000",
    "postinstall": "bower update",
    "prestart": "npm update",
    "test": "./node_modules/karma/bin/karma start karma.conf.js --log-level debug --single-run"
  }
}
